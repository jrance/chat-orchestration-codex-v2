# PR Notes

- **PR-027**: Added a dedicated OpenAI Chat message builder and aligned tool-call payloads so Responses input now maps cleanly to Chat payloads (single system message, assistant.tool_calls preceding tool messages).
